name: Deploy to cloudflare

on:
  push:
    branches:
      - main  # 배포하려는 브랜치에 맞게 수정

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install

    - name: Build and deploy
      run: npm run pages:deploy
